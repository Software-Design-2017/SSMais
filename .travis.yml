language:
  - python

python:
  - "3.6"

sudo: required

install:
  - pip install --upgrade pip
  - pip install -r requirements.txt

script:
  - cd SSMais
  - ./manage.py makemigrations
  - ./manage.py migrate
